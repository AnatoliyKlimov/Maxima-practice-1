import { defaultTodaysProducts, defaultOurProducts } from "@/types/__mocks__";

export const defaultRecommendations = [
	defaultOurProducts[2].id,
	defaultTodaysProducts[2].id,
	defaultTodaysProducts[0].id,
	defaultTodaysProducts[1].id
];
